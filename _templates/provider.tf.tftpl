
# Single-region provider configuration
terraform {
  required_providers {
    cloudstack = {
      source = "cloudstack/cloudstack"
      version = "0.5.0"
    }
  }
}

provider "cloudstack" {
  api_url    = "${api_url}"
  api_key    = "${api_key}"
  secret_key = "${secret_key}"
}
